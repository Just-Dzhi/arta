import { CommandInteraction } from 'discord.js';

async function playSong(interaction: CommandInteraction, query: string) {
    //TODO
};

async function skipSong(interaction: CommandInteraction) {
    //TODO
};

async function stopSong(interaction: CommandInteraction) {
    //TODO
};

async function pauseSong(interaction: CommandInteraction) {
    //TODO
};

async function resumeSong(interaction: CommandInteraction) {
    //TODO
};

async function showQueue(interaction: CommandInteraction) {
    //TODO
};

async function removeSongFromQueue(interaction: CommandInteraction, position: number) {
    //TODO
};

export { playSong, skipSong, stopSong, pauseSong, resumeSong, showQueue, removeSongFromQueue };